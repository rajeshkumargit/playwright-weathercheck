export const WEATHER_SECTION = '#today';
export const CITY_FORECAST_LINK = (city: string) => `a[data-original-title="${city} forecast"]`;
export const DAY_ELEMENT = 'div.day h2';
export const RAIN_TEXT = '.rain em.pop';
